const Joi = require("joi");
const { objectId } = require("./custom.validation");

const createVideo = {};

const getVideos = {};

const getVideo = {};

const updateVideo = {};

const deleteVideo = {};

module.exports = {
  createVideo,
  getVideos,
  getVideo,
  updateVideo,
  deleteVideo,
};
